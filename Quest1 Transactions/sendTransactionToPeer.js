
// /*/ // ⚡

setup =  function (params) {

} 

const t = (f) => tests.push(f)

t(async () => {

    // Simplified test for sendTransaction to peer
    function isSha256(h) {
        const regex = /^[a-f0-9]{64}$/gi
        return regex.test(h)
    }
    return isSha256(txid) 
})

Object.freeze(tests)



